export enum QueueName {
  Cron = 'cron',
}

export enum CronJob {
  Init = 'init',
}

export enum WeekDay {
  Sunday = 'sun',
  Monday = 'mon',
  Tuesday = 'tue',
  Wednesday = 'wed',
  Thursday = 'thu',
  Friday = 'fri',
  Saturday = 'sat',
}
